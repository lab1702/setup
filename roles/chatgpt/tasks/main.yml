---
- name: Locate bundled OpenAI repository signing key
  ansible.builtin.import_tasks: key_file.yml

- name: Install ChatGPT Desktop from the pinned OpenAI repository
  ansible.builtin.include_role:
    name: vendor_repository
  vars:
    vendor_repository_spec: "{{ chatgpt_repository }}"
