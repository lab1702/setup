---
- name: Install VHS from the latest verified GitHub release
  ansible.builtin.include_role:
    name: github_release
    tasks_from: deb.yml
  vars:
    github_release_display_name: VHS
    github_release_repository: "{{ vhs_repository }}"
    github_release_deb_package_name: "{{ vhs_package_name }}"
    github_release_asset_filename: "{{ vhs_package_filename }}"
