---
- name: Install Visual Studio Code from the verified Microsoft repository
  ansible.builtin.include_role:
    name: vendor_repository
  vars:
    vendor_repository_spec: "{{ vscode_repository }}"
