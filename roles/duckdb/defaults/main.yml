---
# DuckDB ships the CLI as a flat zip archive holding a single duckdb binary
# whose --version output starts with the version token.
duckdb_repository: duckdb/duckdb
duckdb_archive_filename: >-
  duckdb_cli-linux-{{ workstation_architecture }}.zip
