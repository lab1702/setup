---
- name: Install Claude Desktop from the verified Anthropic repository
  ansible.builtin.include_role:
    name: vendor_repository
  vars:
    vendor_repository_spec: "{{ claude_desktop_repository }}"
